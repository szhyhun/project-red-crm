Choose a new password.

Someone asked to reset the password for <%= @resource.email %> at <%= @resource.organization.name %>.

Choose a new password: <%= portal_url("/?reset_password_token=#{CGI.escape(@token)}") %>

If you did not ask for this, you can ignore this email; your password stays as it is.
